<div{{ attributes.addClass('result-item-h5p', 'content-box') }}>
    <h3>{{ label }}</h3>
    <div>{{ question_body }}</div>
    <div>{{ score }}</div>
    {#<div>{{ reportHtml }}</div>#}
    {% if content %}
        {% for item in content %}
            {{- item -}}
        {% endfor %}
    {% endif %}
</div>
